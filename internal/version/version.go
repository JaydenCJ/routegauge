// Package version pins the single source of truth for the release version.
package version

// Version is the semantic version of routegauge. It must match go.mod's
// manifest header and the CHANGELOG's newest entry.
const Version = "0.1.0"
